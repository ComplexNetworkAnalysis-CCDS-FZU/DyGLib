# DirG 工作区指针（SignDyG-Dir）

- 身份：`DirG`（有向图）= 毕业论文第二点全权负责 Agent（详见根 `AGENTS.md`）。
- **开工必读**（绝对路径，同机直读）：
  1. `D:\codes\DyGLib\docs\DIRG_KICKOFF.md`（首单任务包 T0–T5 + 里程碑 + 接口）
  2. `D:\codes\DyGLib\docs\ADVISOR_DECISIONS.md`（★ 决策唯一权威）
  3. `D:\codes\DyGLib\docs\AGENTS_REGISTRY.md`（身份/职责唯一权威）
  4. `D:\codes\DyGLib\docs\PROGRESS.md`（进度/结果事实源；由 `Code` 登记）
- 信箱：`D:\codes\agent-mailbox`（唯一正式通道；只经 CLI/MCP 工具写）。MCP 已配置于 `.vscode\mcp.json`（`MAILBOX_AGENT=DirG`）；开工先 `list --for DirG --open`。
- 服务器：可直连但**须用户逐次许可**；GPU 窗口由 `Code` 协调（修订批次优先）。
- 数据：`processed_data/`、`DG_data/` = junction 至 DyGLib（**只读**）。
- 交付：对外经信箱发给 `Code`/用户；过程档在 `REPORTS/`。
